package ru.mail.polis.homework.files;

import java.io.*;
import java.nio.file.Files;
import java.nio.file.Path;

public class CopyFile {

    /**
     * Реализовать копирование папки из pathFrom в pathTo. Скопировать надо все внутренности
     * Файлы копировать ручками через стримы.
     */
//    public static String copySmallFiles(String pathFrom, String pathTo) {
//        Files.walkFileTree(pathFrom, );
//    }
//
//    private static boolean copyFile(Path from, Path to) throws IOException {
//        FileInputStream fileInputStream = new FileInputStream(from.toFile());
//        FileOutputStream fileOutputStream = new FileOutputStream(to.toFile());
//        fileOutputStream.write(fileInputStream.readAllBytes());
//        fileInputStream.close();
//        fileOutputStream.close();
//    }

}
